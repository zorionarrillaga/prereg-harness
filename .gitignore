# ── Vendor market data ────────────────────────────────────────────────────────
# Licensed 1-minute bars, fetched per entitlement and regenerable. Bulky, and
# not ours to redistribute. `python3 bin/factory_data.py pull` repopulates it.
data/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Editors / OS ──────────────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp

# ── NOT ignored, deliberately ─────────────────────────────────────────────────
# research/factory/registry.jsonl and research/factory/findings.jsonl ARE
# tracked. They are the audit trail, and an audit trail nobody can diff is a
# claim rather than a record. Both files here were generated by running the
# harness in this checkout; the originating project's own ledgers are not
# published.
